services:
  backend:
    volumes:
      - ./backend/src:/app/src
      - ./backend/alembic:/app/alembic
    environment:
      DEBUG: "true"
    command: ["uvicorn", "src.main:app", "--host", "0.0.0.0", "--port", "8000", "--reload"]
